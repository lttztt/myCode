# 进度
1. 把vue的项目的底部导航条的路由加了上去。
2. 遇到把之前原生写的轮播和各种js引入和css引入的问题
3. 大致过了一遍vue的文档。还是很多不懂啊。

# 学习
> 看了看npm的文档
```bash
npm install -D / --save-dev 
```
是安装并保存在package.json中开发以来中

而默认的
```bash
npm install
```
是安装并保存翟趐ackage.json 中生产依赖中的

